
# README

Current: The README file is a mess. Did I just add it from the fitted project, and then expand and twist it into its current jumbled mess? Yes, and also yes.

Desired: Uh, definitely more pretty.

Details: More at 11!

# (copied from fitted)

## Hashing for Uniqueness

Current: sha3-512 (via hashlib import in util::chunked_sha3_512_hex(str,int)).

Desired: Specify by argument/configuration input (utilize sha3-512 by default if unspecified).

Details: Maybe a Hasher class, that takes in an hashing algorithm input, with "sha3-512" as the default value.
